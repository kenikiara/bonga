<Application x:Class="VoiceFlow.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- Shared dark theme palette -->
        <SolidColorBrush x:Key="BgBrush" Color="#FF15151E"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#FF1E1E2A"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FF262635"/>
        <SolidColorBrush x:Key="BorderBrushDim" Color="#FF34344A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFF0F0F5"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="#FF9A9AB0"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF7C6CFF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FF9384FF"/>
        <SolidColorBrush x:Key="RecordBrush" Color="#FFFF5470"/>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF44445A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource AccentButton}">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        </Style>

        <Style TargetType="TextBox" x:Key="DarkTextBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="DarkCombo">
            <Setter Property="MinWidth" Value="180"/>
        </Style>
    </Application.Resources>
</Application>
